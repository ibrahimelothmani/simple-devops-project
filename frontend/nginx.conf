server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Important for Angular routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Handle explicit 404s
    error_page 404 /index.html;
}
